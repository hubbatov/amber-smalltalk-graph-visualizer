==================================================================
Server:

- Pharo 1.3
- WebSockets

Client:

- Amber
- WebSocket
- Arbor.js
- Raphael.js

==================================================================

Data structure:

- User
	- firstName				(String)
	- lastName				(String)
	- password				(String)
	- gender				(Boolean)
	- friendList				(OrderedCollection <User>)
	- picture				(Path to image)
	- userId				(String) - (firstName + lastName)

- Client representation
	- node					(GraphNode) - name = (firstName + lastName)
	- edge					(GraphEdge) - scr è dest = (firstName + lastName)
	- addFriend button			(AddEdge)
	- removeFriend button			(RemoveNode)
	- currentUser				(String) - (firstName + lastName)
	- authorization				(Login)
	- exit					(Logout)
	- registration				(Register)


- Server
	- Starting server
	- Stopping server
	- Emit event (Add friend, Remove friend)
	- Commands:
		- Add new user (registration)
		- Add friend
		- Remove friend
		- Authorization
		- Logout